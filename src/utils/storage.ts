import { Appointment, Doctor } from '@/types';\n\nconst DOCTORS_DATA: Doctor[] = [\n  {\n    id: 'dr-omar-faaoury',\n    name: 'د. عمر الفاعوري',\n    specialization: 'العلاج النفسي والإدمان',\n    description: 'متخصص في العلاج النفسي العميق ومعالجة حالات الإدمان والتعاطي',\n    image: '👨‍⚕️',\n    availability: [\n      {\n        date: new Date().toISOString().split('T')[0],\n        times: ['09:00', '10:00', '11:00', '14:00', '15:00'],\n      },\n      {\n        date: new Date(Date.now() + 86400000).toISOString().split('T')[0],\n        times: ['10:00', '11:00', '13:00', '14:00', '15:00', '16:00'],\n      },\n    ],\n  },\n  {\n    id: 'dr-omar-asaad',\n    name: 'د. عمر أسعد',\n    specialization: 'الاكتئاب والاضطرابات والصدمات',\n    description: 'متخصص في علاج الاكتئاب والاضطرابات العامة والصدمات الناتجة عن الكوارث البشرية',\n    image: '👨‍⚕️',\n    availability: [\n      {\n        date: new Date().toISOString().split('T')[0],\n        times: ['10:00', '11:00', '13:00', '14:00', '15:00', '16:00'],\n      },\n      {\n        date: new Date(Date.now() + 86400000).toISOString().split('T')[0],\n        times: ['09:00', '10:00', '11:00', '14:00', '15:00'],\n      },\n    ],\n  },\n];\n\nexport function getDoctors(): Doctor[] {\n  return DOCTORS_DATA;\n}\n\nexport function getDoctor(id: string): Doctor | null {\n  return DOCTORS_DATA.find((d) => d.id === id) || null;\n}\n\nexport function getAllAppointments(): Appointment[] {\n  if (typeof window === 'undefined') return [];\n  const stored = localStorage.getItem('bloom_appointments');\n  return stored ? JSON.parse(stored) : [];\n}\n\nexport function saveAppointment(\n  appointment: Omit<Appointment, 'id' | 'createdAt'>\n): Appointment {\n  const appointments = getAllAppointments();\n  const newAppointment: Appointment = {\n    ...appointment,\n    id: `apt_${Date.now()}`,\n    createdAt: new Date().toISOString(),\n  };\n  appointments.push(newAppointment);\n  localStorage.setItem('bloom_appointments', JSON.stringify(appointments));\n  return newAppointment;\n}\n\nexport function getBookedSlots(doctorId: string, date: string): string[] {\n  const appointments = getAllAppointments();\n  return appointments\n    .filter((apt) => apt.doctorId === doctorId && apt.date === date)\n    .map((apt) => apt.time);\n}\n